# poos

El proyecto crea las tablas necesarias al iniciar la aplicación con el comando:
```
python app.py
```

Debes asegurarte de que las variables de entorno para la conexión a la base de datos sean correctas en el archivo config/database.py, función get_connection().